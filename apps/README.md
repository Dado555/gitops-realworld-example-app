# apps

One Argo CD `Application` manifest per deployable goes here (app-of-apps pattern) — `bootstrap/root-app.yaml` watches this directory.

Nothing real yet. Later phases add `realworld-backend-dev.yaml`, `realworld-frontend-dev.yaml`, `observability.yaml`, etc.

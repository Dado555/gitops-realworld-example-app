# registers charts/realworld-backend w/ argo cd - values inlined via valuesObject, duplicates envs/dev/backend-values.yaml (see step 7.2 report)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: realworld-backend-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:Dado555/gitops-realworld-example-app.git
    targetRevision: main
    path: charts/realworld-backend
    helm:
      valuesObject:
        image:
          repository: "262786914511.dkr.ecr.us-east-1.amazonaws.com/realworld-backend"
          tag: "779708c"
          digest: "sha256:66ed376636e9e0cbc2fb646692318a2b4b8c2a19a381820ea0fa8825fc37e9aa"
        replicaCount: 2
        # step 9.7 alert drill, take 2 - now testing the fixed alert exprs (max_over_time /
        # max by) after confirming both never fired on the first attempt. deployment scaled to
        # 1 replica beforehand to keep the presync migrate job clear of the quota ceiling this
        # time (see the deadlock from take 1).
        drillBadServerPort: true
  destination:
    server: https://kubernetes.default.svc
    namespace: app-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
  # step 7.6: hpa owns replicas once live, don't let selfHeal fight it back to the helm-declared value
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
